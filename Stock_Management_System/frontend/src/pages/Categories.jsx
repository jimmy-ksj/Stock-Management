import{useEffect,useState}from"react";import api from"../services/api";import Layout from"../components/Layout";export default function Categories(){const[d,setD]=useState([]),[name,setName]=useState("");const load=()=>api.get("/categories").then(r=>setD(r.data));useEffect(load,[]);const add=async e=>{e.preventDefault();await api.post("/categories",{category_name:name});setName("");load()};return <Layout><header><div><h1>Categories</h1><p>Organize your products.</p></div></header><form className="panel inline-form" onSubmit={add}><input required value={name} onChange={e=>setName(e.target.value)} placeholder="Category name"/><button>Add Category</button></form><div className="panel table-wrap"><table><thead><tr><th>ID</th><th>Name</th></tr></thead><tbody>{d.map(x=><tr key={x.id}><td>{x.id}</td><td>{x.category_name}</td></tr>)}</tbody></table></div></Layout>}